using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FuzzySet : MonoBehaviour
{
    // Vector of membership functions (animation curves) 
    public AnimationCurve[] membershipFunctions; 

    private void Start()
    {
        //membershipFunctions[0].AddKey(0, 0);
        //membershipFunctions[0].AddKey(0.5f, 1);
        //membershipFunctions[0].AddKey(1, 0);
    }

    // Potential function to create fuzzy set curves via code parameters? 
    // void SetTriangle(float left, float peak, float right); 

    /* void Set(T linguisticVariable)
     * this.Set(
     */
    
}
